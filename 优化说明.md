# 首次加载优化

将本文件夹内的 `index.html` 和整个 `assets` 文件夹一起替换到原网站目录，保持相对路径不变。不需要安装依赖或执行构建。原始 ZIP 未修改。

## 优化结果

本地浏览器在关闭 HTTP 缓存的测试服务上，实际观察到的首页请求为：

| 项目 | 原版 | 优化版 |
| --- | ---: | ---: |
| HTML 与首页素材总大小 | 约 8.24 MB | 约 0.326 MB |
| 首页需要下载的分享脚本 | 5.51 MB | 0（点击分享时才加载） |
| 页面背景 | 1.51 MB | 0.156 MB |
| 吉祥物 | 0.202 MB | 0.138 MB |
| 分享素材包 | 5.51 MB | 1.18 MB（按需加载） |

首页资源体积减少约 **96%**。统计使用十进制 MB，排除测试注入脚本、HTTP 响应头和 favicon；不是声称实际打开时间减少 96%。真实加载时间还取决于线上服务器、网络、缓存和手机性能。未提供线上地址，因此没有验证 DNS、服务器响应时间或 CDN 配置。

## 修改内容

- 移除页面底部同步下载分享素材的脚本标签，游戏逻辑不再被 5.5 MB 下载阻塞。
- 分享素材改为点击后异步加载；复用成功结果，并支持加载失败、超时和无效文件后的重新尝试。
- 分享素材地址添加版本参数，避免复用旧版浏览器缓存。
- 背景转换为高质量 WebP；吉祥物、涂鸦采用无损 WebP。
- 将仅显示为小图标的进度、奖杯图片缩小为 240×160；分享图使用的原尺寸素材单独保留，不改变裁剪坐标。
- 提前发现首页背景和吉祥物，优先请求；未显示页面设为不可见，避免提前加载其背景图，并避免键盘焦点进入隐藏按钮。
- 分享素材删除未被绘制代码使用的奖杯大图。二维码、人物小图和吉祥物在分享包内保持原尺寸、无损编码。
- 分享按钮在图片编码和分享/下载处理完成后恢复，避免重复操作。
- 48 个糖点、比赛晋级逻辑、原有文案与分享 PNG 尺寸（1080×1700）保持不变。

此版本使用 WebP。若你的投放环境必须兼容无法解码 WebP 的旧浏览器，需要另加 PNG 回退版本。

更新部署后应刷新 HTML 缓存；不要只替换 HTML 而漏传素材目录。

## 验证记录

- 浏览器验证首页、开始挑战、选题、撤销、全部比赛轮次、三进二的取消/确认、决赛和前三名展示。
- 最终版本再次检查首页请求，只有背景、吉祥物与 HTML；进入游戏后再加载该屏所需素材。
- 通过本地导出测试页验证生产绘图函数实际生成 1080×1700 PNG，目视核对排版和二维码。测试页禁用了系统分享面板，仅检查本地生成；未向任何联系人发送图片，也未验证微信或系统分享目标。
- 自动检查 JS 语法、48 个糖点数据未变、素材路径存在、异步素材加载合并、错误/超时重试、成功结果复用。
- 分享二维码、人物素材、吉祥物的原始尺寸与无损像素一致性检查通过。
- ZIP 完整性检查通过。测试服务、测试脚本及原始大图未放入部署包。
